my-application-repo/
├── app/
│   ├── src/
│   │   └── ...               # Application source code (e.g., Python, Java, Node.js)
│   ├── Dockerfile            # Dockerfile for building the application image
│   ├── requirements.txt      # Application dependencies (e.g., Python requirements)
│   └── ...                   # Additional app files (e.g., README.md, .env.sample)
├── buildspec.yml             # CodeBuild buildspec file for CI/CD pipeline (if applicable)
└── .gitignore                # Ignore files that shouldn't be committed
